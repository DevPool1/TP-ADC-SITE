---
slug: workflow-git
title: 🍝 Como evitamos "Spaghetti Code" com Gitflow
authors: [andre]
tags: [git, workflow, boas-práticas, trabalho-em-equipa]
date: 2025-12-05
---

Ninguém gosta de esparguete misturado com código. Na equipa Nutri App, a organização do repositório é tão importante como a organização das refeições.

Hoje partilhamos como estamos a usar o **Git** para manter o projeto saudável.

### 🛠️ As nossas Ferramentas
Para gerir este fluxo, utilizamos o essencial:
* **VS Code:** Com a integração nativa de Git.
* **GitHub Issues:** Para distribuir tarefas e organizar o Kanban.

### 🌿 A nossa Estratégia de Ramos

Para garantir que a `main` está sempre pronta para produção (livre de bugs e gorduras saturadas), adotámos um fluxo rigoroso:

1.  **Main:** Código sagrado. Só entra aqui via **Pull Request** aprovado.
2.  **Feature/**: Cada nova funcionalidade (ex: `feature/calculo-imc`) nasce num ramo isolado.
3.  **Fix/**: Para resolver bugs críticos que escaparam à dieta.

### O Processo de Code Review

Antes de qualquer código ser misturado (`merge`), ele passa por uma revisão. É como provar a comida antes de servir aos clientes. Nada entra na panela principal sem o "OK" de outro chef (developer).

#### O que verificamos no PR?
- [ ] O código compila?
- [ ] Variáveis têm nomes decentes?
- [ ] Não há comentários inúteis?


### Exemplo Prático

Aqui está o nosso fluxo habitual no terminal quando iniciamos uma tarefa:
# Verificar status antes

```bash
# 1. Atualizar a base
git checkout main
git pull

# 2. Criar o ramo da funcionalidade
git checkout -b feature/nova-funcionalidade

# 3. Trabalhar e enviar
git commit -m "feat: adiciona lógica de hidratação"
git push origin feature/nova-funcionalidade
```

:::warning Cuidado com os Conflitos 🔥
Tal como na cozinha, por vezes dois chefs tentam mexer na mesma panela (ficheiro) ao mesmo tempo.
Quando temos um *Merge Conflict*, não entramos em pânico: resolvemos em equipa via Discord e garantimos que nenhum código importante é "queimado".
:::

### 📖 Glossário Rápido
* **Commit:** Uma "foto" do código naquele momento.
* **Branch:** Uma linha do tempo paralela.
* **Merge:** Juntar duas linhas do tempo.
